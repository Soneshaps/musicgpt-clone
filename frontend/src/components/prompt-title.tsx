const PromptTitle = ({ title }: { title: string }) => {
  return (
    <div className="mb-6 text-center">
      <h1 className="text-title-1 font-normal">{title}</h1>
    </div>
  );
};

export default PromptTitle;
